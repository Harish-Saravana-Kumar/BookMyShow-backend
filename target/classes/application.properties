spring.application.name=bookmyshow-backend
server.port=${PORT:8080}

# MongoDB Configuration (supports environment variables)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://malarharish007_db_user:niFy0jtVgiiRIqde@cluster0.jtf357u.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:BookMyShow}

# Redis Configuration (supports environment variables)
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.timeout=60000

# Logging
logging.level.root=INFO
logging.level.com.bookmyshow=DEBUG

# CORS Configuration
server.servlet.context-path=/api
